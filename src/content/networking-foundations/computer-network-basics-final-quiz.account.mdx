<h2 id="final-module-quiz">Final module quiz</h2>

Answer all eight questions correctly to complete this assessment.

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_1"
  question="Which statement best explains a network's purpose and the difference between LAN and WAN?"
  options={["Networks let devices exchange data; a LAN covers a limited place while a WAN spans larger distances", "A LAN is always wireless and a WAN is always cabled", "Networks can connect only computers in one room"]}
  correctIndex={0}
  explanation="Network purpose is exchanging data and sharing resources. LAN and WAN mainly describe geographic scope." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_2"
  question="Which is a host?"
  options={["A network printer that sends and receives data", "Only a desktop PC", "Only a router"]}
  correctIndex={0}
  explanation="Any end device that sends, receives, or processes network data can be a host." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_3"
  question="Can the same computer act as both client and server?"
  options={["Yes, different applications can request and provide services", "No, the manufacturer fixes one role", "Only if it is a router"]}
  correctIndex={0}
  explanation="Client and server describe application roles, so one host can perform both." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_4"
  question="Which device comparison is correct?"
  options={["A hub repeats signals, a bridge joins segments, and a switch connects local devices selectively", "A bridge connects countries and a router prints files", "A switch always repeats every signal like a hub"]}
  correctIndex={0}
  explanation="Hub, bridge, and switch represent increasingly selective ways to connect local devices or segments." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_5"
  question="What is the relationship between a router and a default gateway?"
  options={["A default gateway is normally a router interface on the host's network", "A default gateway is the final web server", "A router connects only devices in one physical room"]}
  correctIndex={0}
  explanation="A router connects networks, and one of its reachable interfaces commonly serves as a host's default gateway." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_6"
  question="Which comparison between MAC and IP addresses is correct?"
  options={["MAC supports local-link identity while IP provides logical network addressing", "They are the same address written twice", "IP is a physical connector label"]}
  correctIndex={0}
  explanation="MAC and IP addresses have different formats and responsibilities." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_7"
  question="What do the MAC-address U/L and I/G bits describe?"
  options={["Universal/local administration and individual/group addressing", "User login and Internet gateway", "Upload limit and interface geometry"]}
  correctIndex={0}
  explanation="U/L distinguishes universal from local administration; I/G distinguishes individual from group addressing." />

<KnowledgeCheck progressItemId="computer_network_basics_final_quiz_check_8"
  question="Which OSI-to-TCP/IP mapping and device placement is correct?"
  options={["OSI Network maps to TCP/IP Internet, and a router sits between networks", "OSI Physical maps to TCP/IP Application, and a printer sits between networks", "OSI Application maps to TCP/IP Internet, and a hub is the default gateway"]}
  correctIndex={0}
  explanation="The OSI Network layer aligns with TCP/IP Internet responsibilities, and routers connect different networks." />
