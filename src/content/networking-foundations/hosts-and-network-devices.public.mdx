<h2 id="what-makes-a-device-a-host">What makes a device a host?</h2>

A **host** is an end device that can send, receive, or process network data. A desktop computer, laptop, phone, printer, camera, physical server, and cloud virtual server can all be hosts.

The shape or price of a device does not decide whether it is a host. Its participation in the network does. **A printer with a network interface is a host.**

<h2 id="network-interfaces">Network interfaces</h2>

A host joins a network through a **network interface**. Ethernet is a familiar wired interface, and Wi-Fi is a familiar wireless interface. A laptop may have both.

An interface is a connection point, not another host. Detailed addressing and link behaviour come later in the course.

<h2 id="clients-and-servers">Clients and servers</h2>

A **client application** asks for a service. A **server application** provides that service and responds. When a browser asks a web service for a page, the browser is the client and the web service is the server.

Client and server describe what applications are doing in one conversation. They are not permanent hardware categories: a large computer is not automatically a server, and a phone is not always only a client.

<h2 id="follow-host-conversations">Follow host conversations</h2>

Watch who asks, who provides, and how the response returns. Focus on the role each host has in the current conversation.

<HostRoleConversationPlayer />

<h2 id="one-host-more-than-one-role">One host, more than one role</h2>

One host can run several applications at once. **The same computer can run a client application and serve a file to another device.** Its role depends on what each application is doing in that conversation.

In the file-sharing player, Computer A first requests a file from Computer B. The next conversation reverses those roles. The computers did not change type; their applications changed jobs.

<h2 id="classify-host-roles">Classify host roles</h2>

Select each example to decide whether the application is acting as a client, a server, or both.

<HostRoleClassifier />

<h2 id="knowledge-check">Knowledge check</h2>

<KnowledgeCheck progressItemId="hosts_and_network_devices_check_1"
  question="Which device is a network host?"
  options={["A printer with a network interface", "Only a desktop computer", "Only a dedicated data-centre server"]}
  correctIndex={0}
  explanation="A printer with a network interface is a host because it can receive and process network data." />

<KnowledgeCheck progressItemId="hosts_and_network_devices_check_2"
  question="Can one computer act as both a client and a server?"
  options={["Yes, different applications can perform different roles.", "No, hardware fixes the role permanently.", "Only when the computer has two screens."]}
  correctIndex={0}
  explanation="The same computer can run a client application and serve a file to another device. Client and server are application roles." />
